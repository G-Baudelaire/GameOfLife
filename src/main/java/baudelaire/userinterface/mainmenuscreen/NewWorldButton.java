package baudelaire.userinterface.mainmenuscreen;

import javax.swing.*;

public class NewWorldButton extends MainMenuButton {
    public NewWorldButton(Action action) {
        super("New World", action);
    }
}
